import 'package:vego/core/models/product_model.dart';
// A simplified benchmark since we don't have a real supabase instance with 10k items easily setup in tests.
// We can mock it.
void main() {
  print('Running benchmark');
}
